{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -q '^git commit' && echo '{\"systemMessage\": \"💡 Session break reminder: Consider exiting after 3-5 commits to reduce costs. Check usage with /cost\"}' || true"
          }
        ]
      }
    ]
  }
}
