# Anthropic Bedrock (required for AI draft creation & model editing)
AWS_REGION=ap-southeast-2
AWS_DEFAULT_REGION=ap-southeast-2
AWS_BEARER_TOKEN_BEDROCK="ABSK..."

# Vercel Postgres (required for multi-project support)
POSTGRES_PRISMA_URL="postgres://..."
POSTGRES_URL_NON_POOLING="postgres://..."

# Vercel KV (legacy - being migrated to Postgres)
KV_REST_API_URL="https://..."
KV_REST_API_TOKEN="..."

# Vercel Blob Storage (required for document uploads & artifact storage)
BLOB_READ_WRITE_TOKEN="..."
