import type { ProjectRequirements } from './types'

// Project requirements for BRD Intent
// Add your project-specific assumptions, dependencies, and NFRs here as needed

export const projectRequirements: ProjectRequirements = {
  assumptions: [],
  dependencies: [],
  nfrs: [],
}
